apiVersion: v1
kind: Service
metadata:
    name: "development"
    labels:
        app: "development"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "development"