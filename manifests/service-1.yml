apiVersion: v1
kind: Service
metadata:
    name: "development"
    namespace: default
    labels:
        app: "development"
spec:
    ports:
    - port: 80
      targetPort: 3000
    selector:
        app: "development"
